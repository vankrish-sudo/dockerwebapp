---
version: "3"
services:
 devopsdb:
   container_name: devopsdb
   image: vankrish/docker:db
   ports:
     - "3305:3306"

 application:
   container_name: appcontainer
   image: vankrish/docker:app
   ports:
     - "1235:8080"
   depends_on:
     - "devopsdb"
